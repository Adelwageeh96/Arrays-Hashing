public class Solution {
    public bool ContainsDuplicate(int[] nums) {
        var s= new HashSet<int>(nums);
        if(nums.Length>s.Count){
            return true;
        }
        else{
            return false;
        }

    }
}
